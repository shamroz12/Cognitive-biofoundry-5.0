# in your repo root
cat > requirements.txt <<'REQ'
streamlit==1.25.0
numpy==1.25.2
scipy==1.11.3
matplotlib==3.8.1
pandas==2.1.1
REQ

git add requirements.txt
git commit -m "Use minimal requirements for Streamlit Cloud (safe install)"
git push origin main
